# ── M1: Naive RAG ─────────────────────────────────────────────────────────────
openai>=1.30.0
anthropic>=0.28.0
pypdf>=4.2.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
python-dotenv>=1.0.0
rich>=13.7.0

# ── M2: Hybrid search ──────────────────────────────────────────────────────────
rank-bm25>=0.2.2        # BM25Okapi implementation

# ── M3: Reranking ─────────────────────────────────────────────────────────────
# sentence-transformers already included above (CrossEncoder is part of it)

# ── M4: Query rewriting ────────────────────────────────────────────────────────
# No new deps — uses the LLM API already configured

# ── M5: Citations ──────────────────────────────────────────────────────────────
# No new deps — uses structured output from OpenAI/Anthropic

# ── M6: Evaluation ─────────────────────────────────────────────────────────────
ragas>=0.1.7
datasets>=2.19.0        # HuggingFace datasets (used by RAGAS)

# ── Dev / testing ──────────────────────────────────────────────────────────────
pytest>=8.2.0
jupyter>=1.0.0
numpy>=1.26.0
